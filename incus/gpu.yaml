description: GUI profile for pentest container
devices:
  gpu:
    type: gpu

__GUI_DEVICES__

  opengl-driver:
    type: disk
    readonly: true
    source: __OPENGL_DRIVER_BUNDLE__
    path: /mnt/runtime/gpu
